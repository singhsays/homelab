http:
  routers:
    pve:
      entryPoints:
        - https
      tls: true
      service: grabby
      rule: Host(`grabby.home`)
  services:
    grabby:
      loadBalancer:
        servers:
          - url: https://grabby.home:8006
