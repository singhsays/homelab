api:
  insecure: true
  dashboard: true
  debug: true
entryPoints:
  http:
    address: ":80"
  https:
    address: ":443"
providers:
  docker:
    defaultRule: Host(`{{ normalize .Name }}.home`)
    exposedByDefault: false
    network: services
